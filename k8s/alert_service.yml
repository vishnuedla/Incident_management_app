apiVersion: v1
kind: service
metadata:
  name: alert-notifier-service
  namespace: incident
spec:
  selector:
    app: alert-notifier
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: ClusterIP
