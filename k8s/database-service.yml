apiVersion: v1
kind: Service
metadata:
  name: rabbitmq # lowercase
spec:
  clusterIP: None # headless service
  selector:
    app: rabbitmq
  ports:
    - name: amqp # required, lowercase
      port: 5672
      targetPort: 5672
    - name: management
      port: 15672
      targetPort: 15672
